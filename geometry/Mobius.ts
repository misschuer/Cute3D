class Mobius {
	/**
	 * x(u, v) = (1 + (v/2)*cos(u/2))*cos(u)
	 * y(u, v) = (1 + (v/2)*cos(u/2))*sin(u)
	 * z(u, v) = (v/2)**sin(u/2)
	 * (0 <= u < 2PI), (-1 <= v <= 1)
	 */
	public constructor() {
	}
}